# Local infrastructure

`compose.yaml` at the repository root defines isolated PostgreSQL development and test services. PostgreSQL is the authoritative database; SQLite is not a substitute for PostgreSQL-dependent checks.
