# Repository scripts

Automation added here must be non-destructive, public-safe, and must not embed credentials. Milestone checks are currently exposed through the API `pyproject.toml` and root npm scripts.
