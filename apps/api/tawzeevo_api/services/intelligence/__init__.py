"""Owner-only intelligence (D-089): deterministic, computed on request, never persisted.

Every figure is derived from the canonical rows through the same semantics as the analytics and
ledger services; nothing here is a second financial truth. Results are grouped per currency and
never combined across currencies (D-066).
"""
