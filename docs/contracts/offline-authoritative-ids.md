# Offline authoritative IDs

Offline devices use UUIDs and temporary references for locally created entities. Official invoice and revision business sequences are assigned only by the server after acceptance. Idempotency keys prevent retry duplication. A local reference never becomes authorization.
