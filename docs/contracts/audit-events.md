# Audit event contract

Audit events are append-only records with actor, action, entity reference, tenant scope when applicable, safe structured details, and a server timestamp. Tenant application approval/rejection, access-period changes, lifecycle transitions, and later delivery assignment changes require audit events. Sensitive tokens and credentials are never recorded.
